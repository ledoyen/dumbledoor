[package]
name = "unsafe-macos-process"
version = "0.1.0"
edition = "2021"
description = "Unsafe macOS process management operations with safe wrappers"

[dependencies]
libc = "0.2"
thiserror = "1.0"

[dev-dependencies]
tracing-subscriber = "0.3"