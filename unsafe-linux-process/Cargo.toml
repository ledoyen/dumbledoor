[package]
name = "unsafe-linux-process"
version = "0.1.0"
edition = "2021"
description = "Unsafe Linux process management operations with safe wrappers"

[dependencies]
libc = "0.2"
thiserror = "1.0"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"