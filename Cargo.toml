[workspace]
members = ["process-manager", "unsafe-windows-process", "unsafe-macos-process", "unsafe-linux-process"]
resolver = "2"

[workspace.dependencies]
tracing = "0.1"
thiserror = "1.0"
uuid = { version = "1.0", features = ["v4"] }
tokio = { version = "1.0", features = ["full"] }